{% extends 'base.html.twig' %}

{% set action = app.current_route == 'app_admin_author_new' ? 'Ajout' : 'Édition' %}

{% block title %}{{ action }} d'auteur{% endblock %}

{% block body %}
    <style>
        .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
        .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
    </style>
        <div class="example-wrapper mt-md-5">
            <div class=”w-50 m-auto">
                <h1 class="pt-md-3">{{ action }} d'auteur</h1>
                {{ form_start(form, {'attr': {'class': 'd-flex flex-column mt-4'}}) }}
                {{ form_widget(form) }}
                <button type="submit" class="btn btn-primary align-self-end">Envoyer</button>
                {{ form_end(form) }}
            </div>
        </div>
{% endblock %}